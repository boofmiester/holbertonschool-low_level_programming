#include "main.h"
#include <stdio.h>

/**
 * _abs(int) : a function that computes the absolute value of an integer.
 * @n: the value of the integer
 * Return: Always 0.
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (-n);
}
}
